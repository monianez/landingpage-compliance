'use strict'

const li-document.querySelectorAll('Header-li');
const art-document.querySelectorAll('article');

function activeMenu(){
    let len-act-length;
    while(--len && window.scrollY)
}